Links: [[Estimating Number of Clusters]]

####  definition

not exactly sure how the select is made, but basically you perform the clustering with 1-k clusters and graph the resulting accuracy/error evaluation against the number of clusters used. 

at some point on that graph is likely a point where the "velocity" of the line drastically changes, such that beyond that point, adding new clusters doesn't affect the accuracy

